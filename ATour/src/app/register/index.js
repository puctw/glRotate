import React from 'react';
import { View, Text } from 'react-native';

const RegisterPage = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Regista-te com o Email</Text>
        </View>
    );
};

export default RegisterPage;